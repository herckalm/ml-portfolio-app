# Python / FastAPI
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# .NET / C#
bin/
obj/
*.user
*.suo
.vs/
*.nupkg
TestResults/

# Node / React
node_modules/
dist/
build/
.env.local
.env.*.local

# Environment variables
.env
.env.*
!.env.example

# ML artifacts
*.pkl
*.joblib
*.pt
*.pth
*.onnx
models/

# OS
.DS_Store
Thumbs.db
.env

not-for-git/
**/.gitkeep
Fly_Postgres.doc

# ML model artifacts (binary blobs — never commit)
ml-service/artifacts/

# Local-only compose overrides (machine-specific paths, e.g. real artifact mounts)
docker-compose.override.yml
docker-compose.override.yml.retired

# CI test fixtures — force-track the tiny mini-bundle despite the global
# *.onnx / artifact ignores above (needed by the real-pull CI job).
!ml-service/tests/fixtures/
!ml-service/tests/fixtures/**
