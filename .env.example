POSTGRES_DB=ml_portfolio
POSTGRES_USER=
POSTGRES_PASSWORD=
JWT_SECRET=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
# Absolute paths to the local model export dirs used to seed MinIO once.
# Machine-specific; leave blank unless you're seeding the bucket.
NLP_ARTIFACT_SOURCE_DIR=
CV_ARTIFACT_SOURCE_DIR=
ARTIFACT_BUCKET=ml-artifacts
ARTIFACT_VERSION=1.0.0
